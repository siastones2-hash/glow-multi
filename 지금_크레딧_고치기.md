# 크레딧이 아직 많이 보일 때 (수정 안 된 것처럼 보일 때)

**이유:** GitHub에 수정 코드가 **아직 안 올라감** → Render는 **5월 21일 버전** 그대로 실행 중.

---

## 방법 A — 지금 바로 (DB만 고치기, 1분)

1. [Render Dashboard](https://dashboard.render.com) → glow 서비스 → **PostgreSQL** (또는 Environment의 DATABASE)
2. **Connect** → SQL / Query
3. 아래 실행:

```sql
UPDATE sites SET credit = 0
WHERE credit >= 999999999
   OR (credit * COALESCE(NULLIF(exrate, 0), 1500)) > 10000000;
```

4. 사이트 새로고침 → 크레딧 **₩0** 근처로 보여야 함

---

## 방법 B — 코드 반영 (앞으로 안 틀어지게)

1. `배포_푸시.command` 더블클릭 (푸시 성공)
2. Render에서 **Manual Deploy** 한 번
3. 슈퍼관리자 → **「🔧 비정상 크레딧 0원 정리」** 버튼 (배포 후 보임)

---

## 방법 C — GitHub 웹에서만 올리기

1. https://github.com/siastones2-hash/glow-multi/tree/main/glow-multi
2. `server.js` → Edit → 맥의 `/Users/apple/glow-multi/glow-multi/server.js` 내용 붙여넣기 → Commit
3. `public/index.html` 도 동일
4. Render 자동 배포 대기 (2~5분)
